#include "tsp.h"

using namespace std;

vector<int> tsp(const Graph &graph, int start_vertex) {
    // Return the shortest (with a minimal total weight) path as an array of vertices.
    // Path is assumed to start and end in the given start vertex.
    // Start vertex should be included in the result only once, as the first item.    
    // Return empty array if there is no path.

    // Your implementation here.
    return vector<int> {};
}
