#include "graph_algs_2.h"

std::vector<std::vector<int>> connected_components(const Graph &graph) {
    /// Should return connected components of the graph as array of arrays of vertices.
    /// Should return empty array if there are no vertices.
    std::vector<std::vector<int>> result;
    return result;
}

std::vector<int> shortest_path(const Graph &graph, int start_vertex, int end_vertex) {
    /// Should return shortest path from start to end vertex as array of vertices, 
    /// with start vertex as the first element and end vertex as the last element.
    /// Should return empty array if there is no path.
    std::vector<int> result;
    return result;
}
